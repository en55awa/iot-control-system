# iot-control-system
基于 ESP8266 + PHP + MySQL 的物联网控制系统，支持 HTTP 轮询、多用户隔离、设备共享、组合开关、预设及 OTA 固件更新/ ESP8266 + PHP + MySQL IoT control system with HTTP polling, multi-user isolation, device sharing, combo switches, presets, and OTA firmware updates.
